# Agent Room — خريطة الطريق الموحّدة

> **التاريخ:** 2026-07-16 · **Baseline:** `main @ a37b3c0`
> **المصادر المدمَجة (٤ مستندات، ٢ PR مفتوحة وقت الكتابة):**
> - [`PROJECT_REVIEW.md`](PROJECT_REVIEW.md) — تقرير التقييم الشامل (PR #23)
> - [`LAUNCH_ACTION_PLAN.md`](LAUNCH_ACTION_PLAN.md) — خطة العمل المبنية على التقرير (PR #23)
> - [`CURSOR_INTEGRATION_PLAN.md`](CURSOR_INTEGRATION_PLAN.md) — خطة إضافة Cursor كمزوّد ثالث (PR #23)
> - [`SETUP_DOCTOR_UPDATE_PLAN.md`](SETUP_DOCTOR_UPDATE_PLAN.md) — خطة تقليل تكلفة أول تجربة (PR #24)
>
> **ملاحظة:** الروابط أعلاه بتشتغل بعد دمج PR #23 و PR #24 في `main` — دلوقتي المستندات
> دي موجودة على الفروع البعيدة بس (`origin/docs/project-review-and-debate-test`,
> `origin/docs/setup-doctor-plan`). هذا الملف **مستقل بذاته** ولا يفترض دمجهم.
>
> **الغرض:** المصدرين (تقرير الإصلاحات، وخطة الـ onboarding) اتكتبوا **بمعزل عن بعض**
> على فرعين مختلفين، وفيهم تكرار جزئي (Update Notify). هذا المستند يرتّبهم في **تسلسل
> تنفيذ واحد**، يحل التكرار، ويحدّد لكل بند مصدره.

---

## قرار حل التكرار الوحيد

**Update Notify / قصة إصدار السورس** موجودة مرتين:
- `LAUNCH_ACTION_PLAN.md` → **P1-3** (نسخة مختصرة: `RELEASING.md` + فحص Node)
- `SETUP_DOCTOR_UPDATE_PLAN.md` → **PR3 (Source preflight) + PR4 (Update Notify)** (نسخة أعمق: عزل شبكي كامل، GitHub Releases API كمصدر وحيد، تعليمات حسب طريقة التثبيت)

**القرار:** نعتمد نسخة `SETUP_DOCTOR_UPDATE_PLAN` (PR3+PR4) كتطبيق كامل لـ P1-3. لا تنفيذ مزدوج.

---

## جدول الأولويات الموحّد

| # | البند | المصدر | أولوية | حجم تقديري |
|---|---|---|---|---|
| P0-1 | إلغاء التنفيذ غير ذرّي — الكاتب قد يبدأ بعد طلب Stop | LAUNCH_ACTION_PLAN | 🔴 P0 | 1-2 يوم |
| P0-2 | الواجهة لا تتزامن بعد انقطاع SSE (تفضل "running") | LAUNCH_ACTION_PLAN | 🔴 P0 | ساعات |
| P0-3 | اللغة الافتراضية عربي + نصوص سيرفر hardcoded | LAUNCH_ACTION_PLAN | 🔴 P0 | 1-2 يوم |
| SD-1 | Readiness contract (backend, pure functions) | SETUP_DOCTOR (PR1) | 🟠 عالية | — |
| SD-2 | Setup Doctor UI (تطوير onboarding الموجود) | SETUP_DOCTOR (PR2) | 🟠 عالية | — |
| P1-0 | مسار مساهمة خارجي قابل للتنفيذ (CI بوابة إلزامية) | LAUNCH_ACTION_PLAN | 🟠 P1 | ساعات |
| P1-1 | لا يوجد `fsync` عند الحالات الطرفية | LAUNCH_ACTION_PLAN | 🟠 P1 | ساعات |
| P1-2 | توثيق حدود runtime lock + تحذير مجلد متزامن | LAUNCH_ACTION_PLAN | 🟠 P1 | يوم |
| SD-3/SD-4 | Source preflight + Update Notify (**= يحل P1-3 نهائيًا**) | SETUP_DOCTOR (PR3+PR4) | 🟠 P1 | — |
| P1-4 | قالب البلاغ لا يطلب `/api/diagnostics` | LAUNCH_ACTION_PLAN | 🟠 P1 | دقائق |
| P1-5 | سجلات قراءة الموصلات العالقة "running" لا تُصالَح | LAUNCH_ACTION_PLAN | 🟡 P1 | ساعات |
| P1-6 | لا يوجد اختبار e2e لـ HTTP+SSE ولا لـ `runExecuteAndReview` | LAUNCH_ACTION_PLAN | 🟠 P1 | 2-3 يوم |
| P1-7 | قياس استهلاك المزوّدين (Usage Instrumentation) | LAUNCH_ACTION_PLAN | 🟠 P1 | 2-3 يوم |
| S-2 | إثبات القيمة بأرقام (mini-eval بـ٤ أذرع + GIF) | LAUNCH_ACTION_PLAN | 🔵 S | 3-5 يوم |
| S-1 | إعادة التموضع حول "بوابة المراجعة الأمينة" | LAUNCH_ACTION_PLAN | 🔵 S | أيام (محتوى) |
| — | **🚀 إطلاق ألفا** | — | — | — |
| F-2 | استهلاك التوكن والتكلفة لكل موديل | LAUNCH_ACTION_PLAN | 🟣 ميزة | 2-3 يوم |
| F-1 | مرفقات الجلسة — صور وملفات (PR مستقلة) | LAUNCH_ACTION_PLAN | 🟣 ميزة | 3-5 يوم |
| F-3 | المتبقّي في خطة الاشتراك (spike يحسم الجدوى) | LAUNCH_ACTION_PLAN | 🟣 ميزة | spike + صغير/متوسط |
| CU-0 | Cursor — توصيف CLI + نموذج تأهيل أمني (spike، فرع منفصل) | CURSOR_INTEGRATION_PLAN | 🔵 S | رخيص |
| CU-1 | Cursor كمراجع (بعد P0 + usage baseline) | CURSOR_INTEGRATION_PLAN | 🔵 S | — |
| CU-2 | Cursor كمنفّذ (بعد اجتياز التأهيل) | CURSOR_INTEGRATION_PLAN | 🔵 S | — |
| P2-1..8 | تقسيم `worktree.js` · JSDoc+`checkJs` · توحيد enum التنفيذ · تحقق schema · استخراج آلة حالة `app.js` · وصولية · dependabot · أرشفة الخطط المنتهية | LAUNCH_ACTION_PLAN | 🟡/🟢 P2 | متفرقة |
| S-3 | بروتوكول التقارب كحزمة مستقلة (بعد benchmark) | LAUNCH_ACTION_PLAN | 🔵 S | 3-5 يوم |
| S-4 | قرار الموصلات (إبقاء/نقل) بناءً على بيانات استخدام | LAUNCH_ACTION_PLAN | 🔵 S | 1 يوم |
| S-5 | حسم علاقة "المناظرة" بـ"التعاون" بالـ eval | LAUNCH_ACTION_PLAN | 🔵 S | يوم + eval |

---

## تسلسل التنفيذ المقترح

```
المرحلة 0 — Baseline
  سجّل commit SHA · npm run check + tests كاملة · وثّق النتيجة والتغطية

المرحلة 1 — P0 (لا شيء غيرها قبل ما تُحل) ─────────────── أعلى أولوية مطلقة
  P0-1  إلغاء التنفيذ الذرّي (Stop يوقف الكاتب فعلياً)               🔴
  P0-2  تزامن الواجهة بعد انقطاع SSE                                🔴
  P0-3  i18n مهيكل (لا نصوص سيرفر hardcoded بلغة واحدة)             🔴
  → كل بند يحمل regression test معه

المرحلة 2 — Setup Doctor (أعلى رافعة لتقليل تكلفة أول تجربة)
  SD-1  Readiness contract (backend)
  SD-2  Setup Doctor UI (تطوير الموجود، لا واجهة موازية)
  ← لماذا هنا بالذات: قيمتها صفر قبل حل P0 (مستخدم جديد سيجرّب مرة واحدة
    ويرحل لو Stop/SSE مكسورين) — لكنها أهم من باقي P1 الداخلية لأنها
    بوابة أي مستخدم جديد

المرحلة 3 — باقي P1 (تصليبات داخلية، ليست على مسار أول تجربة)
  P1-0      مسار مساهمة خارجي (CI بوابة إلزامية، hooks اختيارية)
  P1-1      fsync للحالات الطرفية (durable writes)
  P1-2      توثيق حدود runtime lock + تحذير مجلد متزامن (OneDrive)
  SD-3/SD-4 Source preflight + Update Notify  (يحل P1-3 نهائياً)
  P1-4      قالب البلاغ يطلب /api/diagnostics
  P1-5      مصالحة سجلات audit الموصلات عند البدء
  P1-6      اختبارات e2e لـ HTTP+SSE و runExecuteAndReview

المرحلة 4 — إثبات القيمة (قبل أي ميزة أو مزوّد جديد)
  P1-7  قياس استخدام المزوّدين (عقد usage موحّد لكل استدعاء)
  S-2   mini-eval بـ٤ أذرع: Claude فقط · Codex فقط · Manual relay · Agent Room

  ═══════════════════ 🚀 إطلاق ألفا ═══════════════════
  (README + GIF 90 ثانية + S-1 إعادة التموضع حول
   "بوابة المراجعة الأمينة" بدل "مناظرة الوكلاء")

المرحلة 5 — بعد الإطلاق: ميزات وتوسّع (بالترتيب)
  F-2    عرض استهلاك التوكن/التكلفة (فوق P1-7 مباشرة)
  F-1    مرفقات الصور — PR مستقلة، بعد usage + benchmark النصي
  F-3    تتبّع حصة الاشتراك — spike أولاً يحسم الجدوى الفعلية
  CU-0   Cursor: توصيف CLI + نموذج تأهيل أمني (branch منفصل، رخيص،
         يمكن أن يبدأ أبكر بالتوازي — النتائج fixtures فقط، بدون دمج)
  CU-1   Cursor كمراجع (بعد الإطلاق + usage baseline)
  CU-2   Cursor كمنفّذ (بعد اجتياز اختبارات التأهيل الأمني في CU-0/CU-1)

المرحلة 6 — صيانة مستمرة (P2، أولوية منخفضة)
  تقسيم worktree.js (god-module) · JSDoc + checkJs · توحيد enum التنفيذ
  في مكان واحد · تحقق schema لعناصر messages[]/executions[] · استخراج
  آلة حالة من app.js · فجوات الوصولية (تباين، skip-link، confirm) ·
  dependabot/renovate · أرشفة الخطط المنتهية

المرحلة 7 — قرارات استراتيجية بيانات-محورة (بعد الإطلاق بوقت كافٍ)
  S-3  بروتوكول التقارب كحزمة مستقلة (بعد benchmark + استخدام خارجي حقيقي)
  S-4  قرار الموصلات (إبقاء Gmail/Supabase أو نقلها لـ examples/)
  S-5  حسم "المناظرة مقابل التعاون" بالدليل من S-2
```

---

## المبدأ التوجيهي

> **قياس الاستخدام (P1-7) + الـ benchmark النصي (S-2) قبل أي ميزة جديدة أو مزوّد ثالث** —
> عشان كل إضافة (مرفقات، Cursor، تتبّع الحصة) تُقاس أثرها الفعلي بدل ما تتخلط مع بعض
> ونفقد القدرة على معرفة أيّها فرق فعلاً.

أكبر خطر مذكور في `PROJECT_REVIEW.md` هو **"قيمة غير مُثبَتة"** — مش نقص ميزات. لذلك:
1. **P0 أولاً** — بدون هذا، أي مستخدم (قديم أو جديد) يفقد الثقة فوراً.
2. **Setup Doctor ثانياً** — التقرير نفسه يقول: *"الخطر الحقيقي هو التوزيع، مش الجودة."*
3. **إثبات القيمة قبل أي توسّع** — إضافة Cursor أو المرفقات قبل الـ benchmark تعني بناء
   على افتراض، لا دليل.

---

## قرارات معلّقة تحتاج تأكيد المستخدم

- [ ] هل تُدمَج PR #23 و PR #24 كما هما (docs فقط، بدون تعديل) قبل بدء التنفيذ، أم يبقى
      هذا الملف المرجع الوحيد حتى يكتمل التنفيذ؟
- [ ] ترتيب `CU-0` (Cursor spike): هل يبدأ بالتوازي مع P0 (فرع منفصل، تكلفة منخفضة) أم
      يُؤجَّل بالكامل لحد ما بعد الإطلاق كما هو مقترح أعلاه؟
- [ ] `F-1/F-2/F-3` (الميزات الثلاث المطلوبة من المستخدم) — مؤكَّد ترتيبها بعد الإطلاق
      وبعد P1-7، أم فيه بند منها عايز يتقدّم؟
