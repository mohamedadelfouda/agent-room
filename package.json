{
  "name": "agent-room-mvp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local subscription-native shared sessions for Codex CLI and Claude Code",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "check": "node scripts/check-syntax.mjs",
    "test": "node --test",
    "ci": "npm run check && npm test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
