{
  "$comment": "Review Gate config for Agent Room. gateMode 'push' (fires on git push, marker binds to HEAD). Agent Room has no tsc/eslint/vitest yet, so typecheck = the existing 'npm run check' (node --check) and lint/test are disabled until PR-1 adds the test harness + broader check (AR-0.2). When tests land, set test.cmd = 'npm test' and enabled: true, and expand check to all files. perFile:false runs the command once for the whole project.",
  "gateMode": "push",
  "verify": {
    "typecheck": {
      "cmd": "npm run check",
      "perFile": false,
      "enabled": true
    },
    "lint": {
      "enabled": false
    },
    "test": {
      "enabled": false
    }
  },
  "lintableExtensions": ["js", "mjs", "cjs"],
  "codeExtensions": ["js", "mjs", "cjs", "sh", "bash"]
}
